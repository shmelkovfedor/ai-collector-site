# AI-Collector

Голосовой AI-агент для коллекторских агентств. Production landing page.

## Локально

Открыть `index.html` в браузере. Нет build-pipeline.

Для live-reload во время разработки:
```
python3 -m http.server 8080
open http://localhost:8080
```

## Дизайн

Все решения — в `<vault>/Проекты/AI-Collector/DESIGN.md`. Не отклоняться без явного одобрения.

## Деплой

GitHub Pages из `main`-ветки. Push = deploy.
```
git push origin main
```
